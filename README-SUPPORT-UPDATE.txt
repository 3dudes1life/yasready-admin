YASREADY CUSTOMER 4.5.1 — SUPPORT GATEWAY

DESTINATION: CUSTOMER GITHUB ONLY

Admin Business 360 is NOT included or modified.

Adds the customer-facing side of the future support conversation system. Before Cloudflare, messages saved in the chat UI are LOCAL PREVIEW ONLY and explicitly say they are not delivered. Live support falls back to admin@yasready.com.

When the shared backend is connected, this UI should use the same conversations/messages API as Business 360 Admin.
